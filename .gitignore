# Local test suite — not part of the plugin, used for development only
tests/

# Claude Code worktrees
.claude/worktrees/

# Local Claude Code settings (machine-specific, e.g. container permissions)
.claude/settings.local.json
